%section.panel.radius.list
  = content_tag :h3, t('infrastructure.tree_title'), class: 'subheader'
  #infrastructures-tree

  #node-dialog.reveal-modal(data-reveal="")

- content_for :javascript do
  :javascript
    new window.InfrastructuresTree("#{get_sm_dialog_infrastructure_path}", 'node-dialog',
      "#{tree_infrastructure_path}", "#{simulation_manager_records_infrastructure_path}",
      "#{simulation_manager_command_infrastructure_path}")


