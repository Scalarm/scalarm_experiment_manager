#histogram_analysis_link
  = image_tag('histogram_icon.png', :size => '100x150')

#histogram_analysis_window.layered_window
  = image_tag 'close_icon.png', :class => 'close_button'
  = render :partial => 'basic_statistics_charts', :object => @experiment

#rtree_analysis_link
  = image_tag('rtree_icon.png', :size => '100x150')

#rtree_analysis_window.layered_window
  = image_tag 'close_icon.png', :class => 'close_button'
  = render :partial => 'regression_tree_charts', :object => @experiment

#bivariate_analysis_link
  = image_tag('bivariate_icon.png', :size => '100x150')

#bivariate_analysis_window.layered_window
  = image_tag 'close_icon.png', :class => 'close_button'
  = render :partial => 'bivariate_analysis', :object => @experiment

#dialog{:title=>'Dialog Title', :style => 'display:none; overflow-y: auto;'}
  #expanding_dialog_busy{:style=>'text-align: center;'}
    %h5 Loading information about current parameter values...
    = image_tag 'loading.gif'
